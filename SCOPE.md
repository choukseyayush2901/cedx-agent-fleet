# Scope

CASE_ID: `CEDX-XXXX`

Industry: Operations workflow automation.

This repository implements the CEDX Tiny Agent Fleet as a four-agent OOP TypeScript system: Router, Orchestrator, Worker, and Verifier. The implementation uses the supplied `audit.schema.json`, `verify_audit.py`, and seed fixture as source-of-truth contracts.
